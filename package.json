{
  "name": "bridge-crm-vr",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "5-player asymmetric offline WebXR bridge simulator driven by Crew Resource Management",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "bake": "node tools/bake-ao.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "vite": "^6.0.0"
  }
}
